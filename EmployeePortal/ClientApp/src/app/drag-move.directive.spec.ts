import { DragMoveDirective } from './drag-move.directive';

describe('DragMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new DragMoveDirective();
    expect(directive).toBeTruthy();
  });
});
